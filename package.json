{
  "name": "f1-race-strategy-platform",
  "version": "2.0.0",
  "description": "Real-time F1 race monitoring and strategy platform with Python HPC backend",
  "private": true,
  "workspaces": [
    "frontend",
    "shared"
  ],
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../shared && npm install",
    "install:python": "cd python-backend && pip install -r requirements.txt",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd python-backend && python server.py",
    "build:frontend": "cd frontend && npm run build",
    "build:shared": "cd shared && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

